# openvino
Bitcoin price prediction using Deep Learning ( Tensorflow ) and OpenVINO toolkit

## Project details:
- project is using 2 TCN layers ( Non-causal architecture )
- Input is 3D tensor with shape ( batch_size, timesteps, nb_filters )
